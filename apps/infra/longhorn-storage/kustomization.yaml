apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - basic-auth-secret.yaml

helmCharts:
  - name: longhorn
    repo: https://charts.longhorn.io
    version: 1.9.1
    namespace: infra-longhorn-storage
    includeCRDs: true
    valuesFile: values.yaml