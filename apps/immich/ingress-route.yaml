apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: immich
spec:
  routes:
    - match: Host(`photos.sachathommet.fr`)
      kind: Rule
      services:
        - name: immich-server
          port: 2283
  tls:
    certResolver: letsencrypt-prod
